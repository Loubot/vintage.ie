

<div class="col-sm-6 col-md-4">
  <div class="thumbnail">
    
    <a href="<%= photo.photo.url %>" data-lightbox="image-1" data-title="<%= photo.name %>"><%= image_tag photo.photo.url(:thumb), size: "150x150" %></a>
    <div class="caption">
      <h3>Thumbnail label</h3>
      <p>...</p>
      <p><a href="#" class="btn btn-primary" role="button">Button</a> <%= link_to 'Delete',[@item, photo], class: 'btn btn-danger', data: { confirm: 'Are you sure?' }, method: :delete %></p>
    </div>
  </div>
</div>

