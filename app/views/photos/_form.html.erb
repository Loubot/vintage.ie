<%= form_for [@context, @photo], :html => {:multipart => true} do |f| %>
  
  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.file_field :photo %>
  </p>
  
  <p><%= f.submit %></p>
<% end %>